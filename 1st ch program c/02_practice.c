#include <stdio.h>

int main()
{
    int radius = 3;
    float area = 3.14 * radius * radius;
    printf("the area of the circleis %f", area);
    int hight = 6;
    printf("the volum of cylinder is %f", 3.14 * radius * radius * hight);
    return 0;
}