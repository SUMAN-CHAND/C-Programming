#include<stdio.h>

int main(){
    int length,breadth;
    printf("what is the value of length\n");
    scanf("%d",&length);
    printf("what is the value of breadth\n");
     scanf("%d",&breadth);
     printf("the area of your rectengle is %d",length*breadth);
    return 0;
}